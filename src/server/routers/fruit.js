const router = require('koa-router')();
const httpProxy = require('http-proxy');
const config = require('config');
const url = require('url')

router.get(`/:name(${proxyParams.name})/**`, )

module.exports = router;
